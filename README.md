# Joe's GE GitHub Pages

Static GitHub Pages build generated from the Figma concept.

## Publish

1. Put `index.html`, `.nojekyll`, and the `assets/` folder in the root of a GitHub repository.
2. In GitHub, open `Settings -> Pages`.
3. Set the source to the repository branch and root folder.
4. Save and wait for the Pages URL to publish.
